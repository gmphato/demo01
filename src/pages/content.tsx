import {FC} from 'react';

const Content: FC<IProps> = () => {
    const [{ data, resultState, isLoading }, getContentApi] = useApi();
    const makeApi
    return <>
    Content
    </>
}

export default Content;